#!/bin/bash
source /home/user/data.txt
docker run \
    --rm --entrypoint "" \
    -v `pwd`/backup:/backup \
    --link="container:alias" \
    schnitzler/mysqldump \
    mysqldump --opt -h alias -u $login -p$pass "--result-file=/opt/backup/dumps.sql" virtd
